version: '3.8'

services:
  app:
    build: .
    container_name: cvdani-prueba
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=development
